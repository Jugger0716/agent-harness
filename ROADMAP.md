# Agent Harness Roadmap

## Unreleased — `harness-handoff-coldreview-epic-slice` epic (slices A–F)

**Note (added 2026-08-19, v8.11.0 batch):** despite the heading, the epic this table tracks has
already shipped as part of **8.10.0** — this heading itself is stale and is disclosed here rather
than silently renamed, per this table's own append-only convention. The rows below are corrected
in place where a v8.11.0 fix closed them; unclosed rows remain exactly as this epic left them.

Epic closing out cold-review-in-Eval wiring, `/handoff` slice-command chaining, and the two new
SYNC-WITH marker groups that catch a subset of the epic's own cross-reference-rot risk. Full
summary: CHANGELOG.md. This table is a **separate** deferred-items table (2-column, same header
shape as the v8.9 table below by convention, not by merge) — it is intentionally NOT appended to
that older table, which tracks a different, already-shipped release's leftovers.

`skills/test-gen/SKILL.md`/`skills/ship/SKILL.md` gate-option and `conventions.md`
auto-inheritance rows below carry the epic spec's own §유예 항목 table content
(`docs/harness/harness-handoff-coldreview-epic-slice/spec.md`, gitignored — not a public link)
— that table names itself as this table's input, so its 3 rows land here before the slice's own
additional deferrals are appended. The "Item" cell for these 3 rows is an **English restatement
followed by the epic's original Korean Item string, verbatim, in backticks** — a bilingual
**pairing, not a translation substitute** — so an automated substring check against the epic
spec's own Item text (AC-24's verification method) can match the original wording directly
rather than trusting a paraphrase.

| Item | Why deferred |
|------|-------------|
| `skills/test-gen/SKILL.md` framework-selection gate: 6 options → 4 or fewer — epic original: `` `skills/test-gen/SKILL.md` 프레임워크 선택 게이트의 옵션 6 → 4 이하 정합화 `` | Not an in-scope file for this epic; no implementation step touches it; option reduction is a user-facing behavior change needing its own review |
| `skills/ship/SKILL.md` Push Rejected gate: 5 options → 4 or fewer — epic original: `` `skills/ship/SKILL.md` Push Rejected 게이트의 옵션 5 → 4 이하 정합화 `` | Same reasoning; the 5 branches are 5 distinct recovery procedures, not easily merged |
| Epic sub-slices auto-inheriting the parent epic directory's `conventions.md` (§Step 1.5 extension) — epic original: `` 에픽 하위 슬라이스가 부모 에픽 디렉터리의 `conventions.md`를 자동 상속하도록 §Step 1.5 확장 `` | Outside decisions 1–5; the inheritance-scope rule (how far up the ancestor chain) needs a fresh design pass |
| `workflows/_reference/schemas.md` orphaned `sliceHint` delta drift | Epic AC-35 / slice A's own measurement contract owns this file; slice F does not touch it. **Factual half closed 2026-08-19 (v8.11.0 batch, basis: working tree, base `f32c3fb`)**: `workflows/_reference/schemas.md` now carries its own append-only "`sliceHint` delta correction — supersedes the 'Contract delta — `sliceHint`' note above" section (§`sliceHint` delta correction, cited by name), which states the original note's two claims ("not yet produced by any script", "absent from every `PlanResult`") are false as of 2026-08-19 — `PlanResultSchema.required` lists `sliceHint` and both producer and consumer exist. That correction closes the drift's factual half; only the ownership question this row already raised (which file/slice maintains `schemas.md` going forward) remains open |
| `.gitattributes` does not cover `*.md`/`*.py`/`templates/**` | `git ls-files --eol` passing is this machine's `core.autocrlf`, not a repository-level guarantee; widening the pattern set is a separate, deliberate change |
| Epic AC-8 ("Auto-revise re-entry exposed once re-entry + `proposals.json` persistence land") satisfaction judgment | **Updated by the 2026-08-19 integration live probe — the dispatch is no longer unmeasured.** The re-entry ran (runId `wf_66520b0c-89d`): **1 agent / 62,929 tokens against the full run's 3 agents / 130,040 — a 51.6% saving**, with `stats.reSynthesisOnly: true` and Propose actually skipped. slice-f's conclusion is **narrowed, not refuted**: it holds for the 3-persona multi mode it measured (101,645 B); the probe ran `--mode standard` with `conventions="skipped"`, which put the payload at 30,161 B. The real constraint is the orchestrator having to hand-serialise a multi-line document into `args` (size times JSON escaping), not an engine limit. Residual: the probe's payload was a semantically faithful transcription, **not byte-identical**, so "the original was passed by value" is still unproven. Record: `docs/harness/harness-handoff-coldreview-epic-slice/integration-live-probe/probe_report.md` §4.5 (gitignored — not a public link) |
| `specContent` path-shaped alternative (`specPath`) instead of full-text arg | Would shrink the args payload for large specs, but changes the Eval segment's args contract — new design surface |
| §Step 2.6 args should carry the epic spec.md, not just the slice spec | Same category — args-contract redesign, not a slice-f edit |
| Live integration probe — recorded as 7 items, of which this table has only ever named two examples (end-to-end chaining, actual dispatch), so slice F's own probe targets are listed separately here rather than folded into that count: the cold-gate 4-condition fail-closed path (AC-14), the `cold_reviewer.md` ↔ `TPL_COLD_REVIEWER` lockstep (AC-28), the resume gate's chain-into-next-command path, and the `Next :` / `Next cmd:` role split | **The probe ran on 2026-08-19 — this row's "0 live observations" no longer holds.** Recovered: the cold pass executed for the first time in this epic (cumulative 0 to 1; 3 agents against 2 on every prior FAIL_L2 round, cold fields returned), AC-14's 4-condition gate on its **pass-through** path, AC-28's segment-inline `TPL_COLD_REVIEWER` in its shipped path, the resume gate's chaining, and the `Next :` / `Next cmd:` split across all three fallback tiers and all three token-count branches. **Still unmeasured, and narrowed rather than closed**: AC-14's `else if` fail-closed branch (empty `coldFilesList`), AC-28's 2nd gating point (§Step 6 INLINE entry), byte-identity of the two `cold_reviewer.md` copies (the probe assumed it from the synced install, it did not check it), and §Step 7's feedback branch — which this time was **evaluated and returned false** (Critical+Major = 0) rather than being blocked at the gate, so the unobserved span shrank from "before the gate" to "after the condition was evaluated". Full record: the probe report named in the AC-8 row above |
| `conventions-field-contract` SYNC group's slack of 1 (`min_sites=2`, measured 3) | Epic AC-29's slack-0 requirement applies only to the 2 groups this slice adds; tightening a pre-existing group is a separate, deliberate change. **Closed 2026-08-19 (v8.11.0 batch)**: `scripts/verify_sync_markers.py`'s `conventions-field-contract` group now sets `min_sites: 3` (slack 0), with an inline comment recording the 3 real sites and the specific vulnerability of the third (an SSOT prose sentence quoting the marker as a documented example, not an independent sync site) — rewording that sentence drops the count to 2 and fails the group with a message that does not name the real cause. `python scripts/verify_sync_markers.py` exit 0 both before and after; total marker sites unchanged at 44; 2 voice tests (genuine-marker deletion, SSOT-example rewording) each reproduced the predicted `exit 1`, reverted cleanly (`git diff -- <path>` / `git status --porcelain -- <path>` both empty for the touched path — path-scoped, as `verify_log.md` records; an unscoped `--porcelain` could not have been empty, since B1's own edit was already in the tree), re-verified `exit 0` |
| Session Boundary Type A "After Plan" row residual — `/harness generate` typed directly still skips §Step 2.6/§Step 3/§Step 3.5 | §Step Mode Prerequisites' `generate` minimum phase is unchanged (affects every non-epic session) — see `skills/harness/SKILL.md` §Session Boundary |
| `cold_reviewer.md` ↔ `TPL_COLD_REVIEWER` byte-identity has no lint | The sync mechanism (`scripts/verify_block_sync.py`'s BLOCK-START/END SHA256) exists for a different file family (`templates/planner/*.md`); reusing it here was evaluated and not attempted this slice, so the two copies are held in step by one manually run comparison and nothing else — and that comparison covers only the `## Identity` to `## Output Contract` span, 3,712 B on each side, not either file end to end. Carrying BLOCK markers is also not the same as being covered — `GROUPS` currently declares exactly two block tags, both over `templates/planner/*.md` plus their single source, so a marker pair outside that set (`templates/_shared/falsification_rules.md` has one) is never compared |
| `skills/handoff/SKILL.md` §Non-Goals' `skills/migrate/SKILL.md:246`/`:531` absolute line citations | Correcting them requires opening that file and choosing new §section names — out of this slice's own scope; re-judging them risks creating the same kind of cross-reference rot this epic exists to catch. **Closed 2026-08-19 (v8.11.0 batch)**: both replaced in `skills/handoff/SKILL.md` §Non-Goals with §section-name citations into `skills/migrate/SKILL.md` — `§Step 2: Analysis Phase` (parent heading of the INLINE-path WebSearch-fails-fall-back-to-local-sources rule the old `:246` pointed at) and `§Key Rules` (its WebSearch fallback bullet, the old `:531`). Both heading strings verified to exist literally, exactly once, in `skills/migrate/SKILL.md` via `grep -c`. Honest limit disclosed, not claimed away: §citations are not machine-checked in this repository (`SECTION_REF_TARGETS` covers one file, not this one) — this buys rot-resistance, not verifiability |
| Epic AC-22 (`§Session Recovery` `cli_flags.output_dir` drift-detection branch) | Verified **not implemented** — `skills/harness/SKILL.md` §Session Recovery's "Resume" action explicitly says "Do NOT recompute from `cli_flags.output_dir`" (audit/record only); the epic spec's own AC-22 wording was corrected (rev.5) to name the right operand and a non-destructive recommended option, but no code change followed in this slice. **Stale-conclusion corrected 2026-08-19 (v8.11.0 batch, basis: working tree, base `f32c3fb`)**: this row's "no code change followed" is itself now stale — `skills/harness/SKILL.md` §Session Recovery item 6.5 ("docs_path drift check") and item 7-(b) ("docs_path drift") both exist and implement drift detection. This is a **narrowing, not a reversal**, of the row's own quoted claim: "Do NOT recompute from `cli_flags.output_dir`" is still true today — item 6.5 recomputes only from THIS invocation's own `--output-dir` + task arguments, never from the stored `cli_flags.output_dir` field, and writes nothing back into `state.json`. Only the "not implemented" verdict was wrong, not the operand this row correctly named |
| The `## v8.8` deferred table's **P2-3** row cites `skills/harness/SKILL.md` at 2,232 lines; the landed file is larger | That row sits under an already-shipped release heading, and its stale-figure disclosure covers the older number it replaced, not this one. The gap is one of timing, not of basis: 2,232 was measured mid-slice, before later lines landed. **Re-measured 2026-08-19: 2,237 in HEAD and in the index, 2,245 in the working tree** (the working tree carries this release's uncommitted fixes; an earlier revision of this row claimed 2,234 across all three bases, which held for none of them). **Re-measured again 2026-08-19 (v8.11.0 batch, basis: base `f32c3fb`, before this batch's own commits): HEAD and the index are both 2,245 — the "2,237" figure two sentences above was measured against an earlier base and is now stale for HEAD/index too, not only for the working tree.** The byte figure is basis-dependent, though — the same file is about 2 KB larger on disk (CRLF) than in the index (LF). Printing a figure without naming when and against what it was taken reproduces the error that made these disagree. The basis half of that discipline is an open item in this table; no row there asks a figure to name when it was taken, which is the half this row's own gap turned on. **Re-measured again 2026-08-19 (v8.11.0 batch, after ALL of this batch's own body edits to `skills/harness/SKILL.md` — B4 + B7 — landed, working tree, base `f32c3fb`, before this batch's own commits): 2,288 lines.** This confirms the pattern this row already named rather than escaping it: this same batch's edits moved the number again, in the same commit that corrects it. The number is provided only as a dated snapshot — `wc -l skills/harness/SKILL.md` against the actual tree in hand is the only reliable source; no future row should quote this figure without re-running that command first. **Re-measured a fourth time 2026-08-19 (v8.11.0 batch, after every body edit landed in this batch — the original B4 + B7 edits plus this batch's own QA rounds 1–6 fixes (round 3 = the Layer 3 pass, rounds 4–5 = two adversarial re-verification passes, round 6 = the pre-ship cold review) — working tree, base `f32c3fb`, before this batch's own commits): 2,293 lines.** Self-disclosure, stated plainly because the pattern above already proves it: this figure is valid only as of the last body edit to `skills/harness/SKILL.md` landed in this batch, and is invalidated the instant that file is edited again — this row's own figure has already rotted more than once within this single batch precisely because an earlier correction here was followed by a further edit to that file with no re-measurement; this sentence exists to name that mechanism, not merely to report the latest number. **And the prediction held inside this very batch**: the figure above was 2,289 when this self-disclosure was first written, and the batch's own Layer 3 QA round then edited `skills/harness/SKILL.md` again — moving it to 2,291; the first adversarial re-verification round then edited it again, to 2,293; and the second one edited it once more — back to 2,291, the value now printed above. The mechanism is therefore not a hypothetical caution: it fired **four times** inside this one batch, in four consecutive QA rounds, each time invalidating a figure that had just been corrected — the fourth being the pre-ship cold review, which moved it back to 2,293. Note that the figure has now held each of 2,291 and 2,293 twice at different points, so a matching number is not evidence that nothing moved — only a fresh `wc -l` against the tree in hand is |
| `workflows/spec.eval.workflow.js`'s SYNC-WITH marker sits mid-sentence and splits the `// contract` block's noun phrase | The block is one unbroken run of comment lines, so any interior boundary keeps the marker inside it and none of them reads better than where it is. Putting it back above the block's first line returns it to the position this slice already corrected once, where whether it belonged to the `// ---- args` header or to the contract block was not decidable from line order alone. Moving it again would be its second relocation inside one slice, after both placements so far produced an unintended result — so it is left awkward rather than moved to satisfy a lint that already passes and never looks at where a marker sits |
| Slice F's four timing/ordering acceptance criteria (AC-1 baseline capture, AC-4 per-stage lint logs, AC-10 cold-finding triage ledger, AC-16 two-pass cross-reference traversal) | Each requires an observation taken *before* the first edit, so producing it afterwards is fabrication rather than compliance. Two are recorded as unmet by honest disclosure and two only as post-hoc reconstruction; no later edit can move any of the four. Listed here as a closed record, not as schedulable work — the schedulable part is the Step 1 enforcement item in this table |
| Make the baseline capture, the cross-reference traversal, and the finding-triage ledger required `/harness` §Step 1 artifacts, written before the first edit (this merges the same change raised separately as a slice-F handoff follow-up) | The only remedy for the timing/ordering criteria this epic left unmet: they failed because nothing in the contract forced the artifacts to exist at the right moment, and no later document edit can supply a past observation. It is a contract change to `skills/harness/SKILL.md` that would affect every task, not just epics, so it needs its own design pass rather than a drive-by edit in the epic that found the gap |
| A rule that every quoted figure names its measurement basis (diff base, EOL, index vs working tree) | This epic produced three figures that were each right against one basis and wrong against another — a `git diff` read working-tree-vs-index instead of vs HEAD, an EOL filter matched only the index field and missed working-tree CRLF, and `wc -c` moved ~2 KB with the line ending. The tool behaved correctly all three times; the basis was chosen wrong. Where the rule should live (a `templates/_shared/` single source vs. per-skill prose) is undecided |
| AC-20 / AC-30 disclosure precision — the before/after wording of the four SSOT corrections, and the line-number → current-content comparison behind the AC-30 verdicts | Both are recorded as partially unmet and stay that way: `docs/` is `.gitignore`d in this repository, so no earlier revision of those planning documents survives to quote, and the raw comparison output was never captured. Redoing the comparison now would sharpen the disclosure without changing either verdict |
| AC-26 (scope containment) — `workflows/harness.eval.workflow.js` landed 6 hunks against a declared 4 | Counted as unmet rather than rescued by revising the spec after the round limit had passed. The 2 extra hunks are adjacent comments that the 4-condition gate change made false on the spot, so reverting them would leave 2 stale comments — the exact rot this epic exists to catch. Sharpening the disclosure changes the record's accuracy, not the verdict |
| Absolute line-number citations left in this epic's own outputs — 10 in the slice `changes.md` and 12 sites in `name_manifest.md` (both gitignored — not a public link) | The slice's own rule is to cite by §section name, and its outputs break it, the triage ledger that adjudicated the rule included. 7 of the 12 `name_manifest.md` sites were measured stale (working tree at epic close: the cited line now holds unrelated content), so each needs a fresh §section name chosen by opening the target file — which is how this class of citation rots in the first place. Separate from the `skills/handoff/SKILL.md` §Non-Goals citations, which have their own row |
| **Cold review is blind to any defect the spec has sanctioned** — a spec that declares a known deviation makes it invisible to the cold pass, because the cold reviewer receives `specContent` by design (epic §결정 2). The 2026-08-19 probe measured this directly: a file carrying four statements the change had just made false returned `coldStatus: "clean"` with 0 findings, because the spec had pre-approved all four. `clean` may be contractually correct — a sanctioned deviation is not a defect — but the adversarial value of the cold pass is bounded by the spec's own blind spots, and a spec author can neutralise it without meaning to. Counted as AC-33 anchoring sample 2 of 3-5, pointing the opposite way from sample 1 | Deciding what to do needs a design pass, not an edit: withholding the spec breaks the diff-plus-spec contract the cold pass is built on, while keeping it leaves this hole. Recorded with both readings intact rather than resolved in one direction. Two starting candidates, recorded so the design pass does not begin from zero: (a) hand the cold reviewer the list of deviations this change deliberately takes, rather than the whole spec; (b) keep the spec but split the counters, so a spec-sanctioned deviation and a reviewer-found one are reported separately and a `clean` result shows which kind it is |
| **Plan Critic has run-to-run variance on an unchanged document** — two consecutive "Run Critic anyway" dispatches against a spec.md whose mtime did not move returned C3/M7/m5 and then C3/M8/m7 | slice-e (5 rounds) and slice-f (3 rounds) both recorded non-convergence, but the document changed between their rounds, so "fixes open new layers" and "the measurement itself varies" could not be separated. Holding the document constant separates them: both components are real, and the variance component means "more rounds converge to PASS" cannot hold in principle. **Mechanism attribution corrected 2026-08-19 (release-readiness review):** an earlier version of this cell tied the finding to the cold pass's `verdict === 'PASS'` gate. That is a different stage — the literal lives in `workflows/harness.eval.workflow.js` and reads the **Eval evaluator's** verdict, whereas the Plan Critic segment returns no `verdict` field at all and is consumed through `counts.critical` / `counts.major`. The measurement stands (same bytes in, different counts out); the design question it actually raises is about those count conditions. Auto-revise is bounded to one round by §Auto-revise Exposure Predicate point 3, so the variance does not burn rounds — which is why this is a design decision to schedule, not a fix to rush |
| **The Auto-revise re-entry launders `proposals.json` through the orchestrator's transcription** — the contract says to overwrite it with the segment's returned `proposals`, but on that path the segment returns the `priorProposals` it was handed, so whatever the orchestrator serialised becomes the authoritative file. Observed once (2026-08-19, one run, across a resume): 15,807 B to 10,793 B — mechanism never reproduced, so this is a dated observation rather than proof of cause; the fix rests on the code fact that the re-entry path returns `A.priorProposals` with only falsy elements removed | **Deferral reason corrected 2026-08-19 (release-readiness review).** The earlier reason said a lossless re-entry needs the orchestrator to pass the original bytes, making the fix the `specPath`-style args redesign. That overestimates the cost: the loss comes from the write, not the transfer. `harness.plan.workflow.js` initialises `proposals` from `A.priorProposals` and returns that same array on the re-entry path, while `skills/harness/SKILL.md` §Step 2 — WORKFLOW path instructs the orchestrator to overwrite `.harness/planner/proposals.json` with the returned value even though it is known to be unchanged on that path. Narrowing that one instruction — "on the re-entry path do not rewrite proposals.json; the first dispatch's file stays authoritative" — removes the loss without touching the args contract. A second, smaller channel is `.filter(Boolean)`, which drops null elements on the round trip. Still deferred, but for a different reason than before: it edits a live re-entry contract and wants its own verification round. The args-payload redesign above stays a genuinely separate item. **Closed 2026-08-19 (v8.11.0 batch)**: `skills/harness/SKILL.md` §Step 2 — WORKFLOW path's Auto-revise re-entry paragraph now instructs exactly the narrowed rule quoted above — `reSynthesisOnly: true` re-entries no longer rewrite `.harness/planner/proposals.json`; the first dispatch's file (§Step 2 item 5) stays sole-authoritative for the session. The `(kept authoritative even though unchanged on this path)` phrase is gone. The FULL-re-run branch (proposals.json validity check fails) is explicitly carved out in the same paragraph: it still runs §Step 2 item 5's write rule in full, because that branch's Propose step genuinely produces new proposals — narrowing, not a blanket "never write". All 5 B4 ripple points re-read for consistency (the file's own 'You do NOT' summary line, §Architecture Principles #1's write note, §Step 3 Auto-revise Exposure Predicate point 2, §Step 2.6's 'interrupted before its write', and `workflows/harness.plan.workflow.js`'s args-contract comment) — none contradicted the new rule; none needed edits. **Corrected 2026-08-19 (adversarial re-verification)**: there were **six** sites, not five — the sixth is cross-file, `workflows/harness.plan.workflow.js`'s `proposals` **return** comment, which described the orchestrator persisting the returned array unconditionally. B4 makes that false, so it was narrowed in place to the first dispatch and the FULL re-run and now cites `skills/harness/SKILL.md` §Step 2 — WORKFLOW path by name. CHANGELOG records the same correction |
| **§Step 3 Pass A's "Run Critic anyway" paragraph carries a false universal** — it states the re-presentation will *never* re-land on the same ①-b/**①-c**/②-b/③ row for the same spec.md *because* a completed own dispatch *always* writes a fresher mtime. That reasoning closes the staleness axis only. Row ①-c's non-exposure cause is the `round` bound, and `round` is incremented solely by the write that follows an Auto-revise re-entry (§Step 2.6's single-write note), so a critic re-run cannot reset it | The probe predicted re-landing on ①-c before pressing the option and then observed exactly that. Narrow the claim to ①-b/②-b/③ and state that ①-c re-lands whenever the `round` bound is the cause — at which point Auto-revise is permanently unavailable for that spec.md. Deferred because this epic's own Do NOT forbids blanket quantifiers, so the rewrite should be checked against every other `never`/`always` in the same file rather than done in isolation. **Narrowing half closed 2026-08-19 (v8.11.0 batch, basis: working tree, base `f32c3fb`)**: `skills/harness/SKILL.md` §Step 3 `#### Pass A` already carries the exact narrowed claim this row asks for — a separate "**Exception — row ①-c.**" paragraph now states "①-c DOES re-land whenever point 3 [the `round` bound] is the cause", and that same paragraph explicitly defers the second half of this row (a full pass over the file's OTHER blanket `never`/`always` claims) rather than closing it. That second half is still open — raw, case-insensitive `never`/`always` occurrence count in `skills/harness/SKILL.md` on the same basis: 152 (129 `never`-family + 23 `always`-family via `grep -oiE '\bnever\b\|\balways\b'`); most are narrowly-scoped prohibitions, not blanket-quantifier claims, but this is the unaudited surface size, not a small one |
| **§Step 3.5's `In scope` range split has no defined algorithm** — the column source says to split `PlanResult.steps[]` into `n` contiguous ranges without saying how. For 8 steps over 3 slices, a mechanical even split (1-3/4-6/7-8) and a content-aligned split (1-4/5-6/7-8) give different answers, and the even split contradicts the candidate's own stated layers | The probe used the content-aligned split and disclosed the basis under the table. Specifying it needs a rule that reads the candidate's `slices[]` semantics, which is a design pass rather than a wording fix. Starting candidate: order by the step numbers each candidate slice already names, instead of dividing the range by slice count |
| **`Slice` id generation is non-deterministic by construction** — the rule demands an `<English raw kebab summary>` while the source `candidates[].slices[]` is prose in `user_lang` (Korean in the probe), so the step is translate-and-summarise, not slugify. The three ids generated passed all five constraints, but a different orchestrator could produce different English from the same Korean and nothing in the contract bounds that spread. The `-2`/`-3` duplicate rule and the over-50-character path did not occur naturally and stay unmeasured | Constraining it means either fixing the summary source (for example requiring an English label in `candidates[]`) or accepting the spread and saying so. Both are contract changes to the plan segment's return shape. Starting candidate: add an English label field to `candidates[].slices[]` so the id becomes a mapping rather than a translate-and-summarise step |
| **`/handoff`'s Progress Ledger does not say whether the backticks around a `Slice` cell are part of the value** — the reference implementations all write the id in backticks, and the `Next cmd:` derivation has an explicit "backticks are Markdown delimiters, not part of the extracted string" literal, but the `Next :` tier-1 rule has no counterpart | One sentence in `skills/handoff/SKILL.md`, but it belongs with the next edit to that derivation rather than on its own. **Closed 2026-08-19 (v8.11.0 batch)**: `skills/handoff/SKILL.md` §Step 5's `Next :` tier-1 rule now strips exactly one outer backtick pair from the ledger's `Slice` value (using it verbatim, unstripped, when the backticks are absent or unbalanced), reader-side only — it does not change the format `generate` writes, does not affect the `Next cmd:` byte-identical rule, and tier-2 (Docs-path-derived, not a ledger cell) is explicitly out of scope. Residual disclosed in the same sentence rather than silently left open: the ledger's `Epic` cell can carry the same backtick ambiguity and is read elsewhere in this skill for carry-forward selection (`generate` Step 1 item 5's Epic-matching rule) — this rule does not extend there; see the new row below |
| **Chaining from `/handoff resume` cannot test the tool-scope leak hypothesis** — `/harness` interposes at least one AskUserQuestion (model config; two if CLAUDE.md is thin) before its first Workflow dispatch, so the turn always splits before the call the hypothesis is about. The 2026-08-19 probe chained successfully into `/harness` and its Workflow launched, but across a turn boundary, so it is not evidence either way | A clean test needs a session with an `agent-harness-defaults:` line **and** a rich CLAUDE.md, so both questions are suppressed and the dispatch lands in the same turn as the chain. `templates/_shared/mode_gate.md` rule 3 cause (a) is left unchanged — the probe deliberately did not update its observation record |
| **`render()`'s sequential substitution lets a disk-sourced payload fill later `{placeholders}`** — `harness.plan.workflow.js` substitutes vars in insertion order, and on the Auto-revise re-entry path `all_proposals` is built from `priorProposals`, which the orchestrator reads from `.harness/planner/proposals.json` — a file a human can edit between rounds. A literal `{critic_findings}` or `{task_description}` placed there is substituted on a later pass. §Auto-revise Exposure Predicate's validity check covers existence, JSON parse, array shape, non-emptiness and per-element persona/summary; it does not neutralise placeholders | Impact is bounded: both values are documents already present in the same prompt, so this duplicates rather than exfiltrates, and the source is a local `.harness/` file. The fix is a single-pass replacer — and it has to stay a replacer *function*, since this file's own comment records the `$&`/`$'` hazard that made split/join the convention here. **Measured 2026-08-19: the same `const render = (tpl, vars) =>` idiom is inlined in 15 workflow scripts, not the 2 the finding assumed.** Changing one or two leaves the family inconsistent, and the other 13 were outside this review's scope — so this is a family-wide change needing its own review round, not a drive-by edit |
| Quantitative claims in this table cite evidence that does not ship — runIds, token counts, byte sizes and critic count triples whose only record is `docs/harness/harness-handoff-coldreview-epic-slice/integration-live-probe/probe_report.md`, and `docs/` is `.gitignore`d | ROADMAP.md is committed, so the claims are permanent while their basis is local-only and unreproducible by anyone else. This is not hypothetical in this table: the AC-20 / AC-30 row already records that no earlier revision of those documents survives to quote. Mitigated in part by inlining the decision-driving figures in the row text, so a row stays usable without the file. Deferred is the larger fix — committing the measurements to a tracked path (this repository already uses `workflows/_reference/` for that) and splitting this table's two roles, since several rows now record probe findings rather than deferred work |
| `harness.plan.workflow.js`'s args contract comment interleaves the current contract with `an earlier revision said X` corrections | Keeping a superseded claim beside the code is this repository's deliberate anti-rot practice, not an accident — `workflows/_reference/schemas.md`'s append-only rule and its correction notes are the same pattern, and 'git blame covers it' is the argument this epic disproved (nobody reads blame, which is why the stale claims survived to be found). The corrections therefore stay. Deferred is only the readability half: collecting the revision notes into one section at the end of the comment block so the contract itself reads straight through. **Closed 2026-08-19 (v8.11.0 batch)**: the 3 "An earlier revision of this comment ..." notes (reSynthesisOnly/priorProposals; criticFindings dual-doc; both-gates truthy) are now collected verbatim, unrewritten, into one section at the end of the args-contract comment block, each prefixed with a `[keyword]` identifying its original bullet — move only, not a rewrite. `node scripts/check_workflow_syntax.mjs` and `python scripts/verify_meta_literal.py` both exit 0 after the move; `git ls-files --eol` confirms `i/lf w/lf attr/text eol=lf` unchanged; the `HARD GATE` (spaced) token, the args parse guard line, and the separate `critic-revision-block` SYNC marker were not touched |
| `§Step 2 — WORKFLOW path`-style section citations into `skills/harness/SKILL.md` are covered by no lint | `scripts/verify_sync_markers.py`'s `SECTION_REF_TARGETS` holds a single file (`workflows/_reference/study_measurements.md`) and its `SECTION_HEADING_RE` matches `## §…` only, so a `####` heading carrying no `§` cannot match. Adding the file and widening the regex would bring these citations under the lint, but the script's own comment warns that a generic check false-positives across this repository's heading styles — a design judgement with a real cost, not a config tweak. Until then these citations are held by review alone, which is what this row discloses |
| §Ambiguity Prompt's ultracode-UNDETERMINED exception lives only in the SSOT — the 9 consuming `skills/*/SKILL.md` restatements still describe the base rule (scope-advised tier, else shallowest) with no mention of it | Behaviour is determined: each restatement names `templates/_shared/mode_gate.md` as the single source, and the SSOT carries the exception. Adding the exception to 9 files would widen the restated surface this epic spent two slices narrowing, so the fix is not 'copy it everywhere' — it is deciding whether the `(Recommended)` rule should be restated in consumers at all, or cited. Flagged by both cold reviewers at the 2026-08-19 release gate; recorded rather than patched under a release |
| `skills/ship/SKILL.md` Stage 2 Pass 2 prescribes re-serialising `.claude-plugin/*.json` while 'preserving key order, indentation … CRLF, BOM and trailing-newline' — that is not achievable with a JSON round trip, because no `json.dumps` setting preserves an **inline array**. Observed on this release (2026-08-19): following the instruction literally turned `plugin.json`'s one-line `keywords` array into 30 lines, making a 3-line version bump a 61-line diff. The run reverted and applied a key-anchored line replacement instead (3 lines changed), after first verifying that every literal occurrence of the old version in those two files sits on a `\"version\":` line | The step's intent is right — a naive repo-wide string replace would corrupt unrelated fields, which is the regression the step exists to block. What is wrong is the prescribed mechanism. The fix is to replace 'parse → mutate → serialise' with 'locate the key path, then edit that value in place', keeping the JSON parse as a **validation** step (before and after) rather than as the write path. Deferred rather than patched mid-release: it edits the release tool while that tool is running. **Closed 2026-08-19 (v8.11.0 batch)**: `skills/ship/SKILL.md` §Pass 2's `.claude-plugin/*.json` steps now do exactly this — step 2's parse is a read-only oracle (validity + current-value capture, never re-serialized), step 4 anchors by key path and edits one line's value token in place (`"version":` first-match explicitly banned, with `marketplace.json`'s `$.metadata.version`/`$.plugins[i].version` same-literal collision named as the reason; ambiguity after object-span narrowing routes to the existing skip/abort question), and step 5 adds a byte assertion (every line except the ones actually rewritten, plus total line count, must match the step-1 original) with an explicit failure path — restore this Pass 2 run's written files from their step-1 raw-byte backups, abort, block every later stage — Stage 3 (`changelog`) onward, not only the irreversible ones (`git_ops` / `gh_release`) — report; if the restore itself fails, halt the whole session via a new `substep` value rather than let Session Recovery silently re-enter over a partially-mutated file. Step 3's 4-way branch and the Important regression-blocker paragraph are unmodified (1 sentence added to the latter). **Honestly disclosed, not claimed**: this procedure ships unexecuted — its first real run is the next `/ship` invocation, which is also the release that ships this very change; the manifest version bump itself stays `/ship`'s job, not this batch's |
| Repository ↔ installed plugin copy drift — **re-measured 2026-08-19: EOL-only pairs 0, content-differing files 8, missing 0**, over 119 tracked files against each of the two install locations (`~/.claude/plugins/marketplaces/agent-harness-marketplace/` and `~/.claude/plugins/cache/agent-harness-marketplace/agent-harness/8.9.0/`) | This row previously read "13 EOL-only differing pairs … content-identical", measured 2026-08-18. Both axes of that figure are now stale: the EOL pairs were cleared by that day's sync, and the 8 content-differing files are this release's own edits, which the release-time re-sync clears. Recorded rather than silently overwritten, because the earlier figure is what the AC-28 byte-identity row leans on when it says the probe "assumed it from the synced install". Any number on this row is true only until the next repository edit — it carries its measurement date, and the instruction is to re-measure rather than to quote it. **Correction (2026-08-26, `review-fixes-v8-12` S5).** This row's standing instruction — re-measure rather than quote — now has a named command: `/harness doctor` reports this comparison as diagnostic item ④ (c). Three things it does **not** change: the figures above stay as the dated record they are; doctor writes nothing, so no re-sync follows from running it; and the cache path named in the Item cell is one version directory among several the cache holds at once, so the copy to compare is resolved at measurement time rather than quoted from this row. This row stays open — only a re-sync closes the drift itself |
| Two revision-bookkeeping drifts in this epic's planning documents (gitignored — not a public link) — `slice_plan.md`'s `Requirements source (SSOT)` row still names rev.3 while the spec body is at rev.5, and the epic `spec.md`'s revision history stops at rev.4 while its own AC text carries rev.5 corrections | Neither file ships and no lint reads them, so the drift costs nothing at release and costs only the next reader, who would take each header's revision at face value. Fixing it is bookkeeping on documents this epic has closed, and editing an SSOT after the work it governed is finished is itself a risk this epic learned to avoid |

**New deferred items (added 2026-08-19, v8.11.0 ROADMAP-deferred batch — 8 items the batch's spec
scoped explicitly out, plus 1 more surfaced from B3's own `Slice`-only scope decision and 1 more
surfaced while measuring that batch's own AC-17a/17b baseline):**

> **Correction (2026-08-24).** The count above describes only the rows this table opened with. Later epics append here rather than starting a table of their own, and every such row names its own slice in bold at the start — `review-fixes-v8-12` S3 added 2 and S4 added 5. Read the row prefixes, not this paragraph's count.

| Item | Why deferred |
|------|-------------|
| `skills/spec/SKILL.md` Phase 2d-D's re-synthesis re-entry writes `.harness/spec/proposals.json` **verbatim** on every re-entry — the same byte-parity design B4 narrowed for `/harness`'s own re-entry, left as-is here | Explicitly out of scope for the v8.11.0 batch (a byte-identical twin edit, not touched) — `/spec` and `/harness` now have two different rules for structurally the same situation, disclosed here rather than silently diverged |
| The other 6 skills sharing the `{Resume, Restart, Stop}` 3-option Session Recovery gate (`debug`, `migrate`, `refactor`, `ship`, `spec`, `test-gen`) do not carry the non-destructive `View state only` 4th option B7 added to `/harness` only | B7 was scoped to `/harness` deliberately (removing a destructive-only default, not proliferating options) — extending it to 6 more gates each with their own status-block shape and field set is a design pass, not a copy-paste |
| `skills/harness/SKILL.md` §Session Recovery item 7-(a) Epic residue branch still offers only `{"Restart", "Stop"}` — both destructive | Its option set is unmodified by this batch on purpose (spec's explicit out-of-scope decision) — only its `minus "Resume"` cross-reference parenthetical was extended to `minus "View state only"`, since the new 4th option would otherwise have falsified it; disclosed here so the inconsistency with the new `Otherwise`-branch 4th option is not mistaken for an oversight |
| Three absolute line citation sites (across 2 targets) left unfixed: `skills/harness/SKILL.md`'s `` `skills/spec/SKILL.md:166` `` and `workflows/_reference/schemas.md`'s two `` `skills/harness/SKILL.md:704` `` citations. **Corrected 2026-08-19 (adversarial re-verification, basis: working tree, base `f32c3fb`)**: an earlier revision of this row called all of them "already-rotted" and tagged `skills/spec/SKILL.md:166` "(already off-by-one)" — measured, that one still **resolves**: line 166 of `skills/spec/SKILL.md` (a file this batch does not touch) does carry the `last_findings_path` null-safe / file-not-found guard that the citing sentence says it mirrors. It is deferred for being an absolute line citation, not for being rotted. The two `` :704 `` citations are genuinely rotted. The count also disagreed with its own enumeration ("Two" over three sites) and with CHANGELOG's "3" | Out of this batch's scope by the spec's own decision. Of the three sites, the two `` :704 `` citations are already-stale; `` `skills/spec/SKILL.md:166` `` still resolves. None of the three was newly broken by this batch's edits — counted honestly as pre-existing, not hidden by AC-17a's "new citations = 0" claim. **Partially closed 2026-08-24 (review-fixes-v8-12 S3):** the `` `skills/spec/SKILL.md:166` `` site is fixed — that slice inserts lines into `skills/spec/SKILL.md` above 166, which would have rotted it, so the citation in `skills/harness/SKILL.md` §Step 2.6: Plan Critic was replaced with `skills/spec/SKILL.md` §Session Recovery plus the string `null-safe guard`, measured to occur once in that file. The two `` :704 `` citations in `workflows/_reference/schemas.md` remain deferred and are NOT closed: the first is a rotted active claim its own file's append-only rule preserves unmodified, and the second already supersedes it with a §Section citation. S3's harness edits shifted line 704 further without making either any more or less false |
| `templates/_shared/askuserquestion.md`'s per-call option-cap bullet carries a `` `skills/test-gen/SKILL.md:142-150` `` absolute line citation | Not disturbed by this batch's edits (unlike the same line's `skills/ship/SKILL.md` citation, which B5's edit did rot and this batch did fix) — left as its own pre-existing item, not silently rescued alongside the one that actually needed it |
| `scripts/verify_sync_markers.py`'s `SECTION_REF_TARGETS` covers exactly one file (`workflows/_reference/study_measurements.md`); §section-name citations elsewhere (including the ones B2 just added) are not machine-verified | Widening the regex/target list is a design judgement with real false-positive cost across this repository's varied heading styles (the script's own comment says so) — this batch buys rot-resistance via §citation, not verifiability, and discloses that limit rather than overclaiming it |
| P2-1's second half — an explicit priority rule between `/harness` Session Recovery's gate and the no-args next-step suggestion at §CLI Parsing | B7 (this batch) closed only the non-destructive-option half of P2-1; the ordering/priority rule between two separate suggestion surfaces is unrelated design work |
| A1's second half — a full pass over `skills/harness/SKILL.md`'s other blanket `never`/`always` claims beyond the one this batch's A1 row corrected | Raw surface size measured this batch (2026-08-19, working tree, base `f32c3fb`): 152 case-insensitive `never`/`always` occurrences: not all are blanket-quantifier claims needing review, but the unaudited surface is not small — a dedicated pass, not a drive-by continuation |
| `/handoff`'s Progress Ledger `Epic` cell has the same backtick-or-not ambiguity B3 fixed for the `Slice` cell — but `Epic` is a carry-forward **selection key** (`generate` Step 1 item 5-(b)'s "has an `Epic` column value matching the confirmed Epic" rule), so an unstripped/mismatched backtick there risks a silent wrong-epic carry-forward, not just a display glitch | B3 was scoped by spec rev.2 §M12 to the `Slice` cell only; extending the same one-sentence fix to `Epic` (or explicitly excluding it with a stated reason, as this row now does for the whole table) needs its own small edit + verification pass, flagged here rather than silently left asymmetric |
| `skills/handoff/SKILL.md`'s §Step 3.5 reduced-check paragraph cites `` `spec.md:407` `` — an absolute line citation into a `docs/`-gitignored file this repository cannot resolve or verify (path ambiguous: no tracked `spec.md` in this repository matches) | Surfaced while capturing this batch's own AC-17a pre-edit baseline (`docs/harness/roadmap-deferred-v8-11/verify_log.md`, gitignored — not a public link); out of scope to fix (the cited document is not in this repository), but omitting it from the citation count would make AC-17b's "enumerate every remaining citation" claim false by omission |
| **`skills/harness/SKILL.md` §Session Recovery item 2's legacy branch has no non-interactive default** — added 2026-08-24 by `review-fixes-v8-12` S3. `/ship`'s equivalent branch got one in that slice ("a session that cannot present an interactive prompt → halt on this path too"), and `templates/_shared/session_conflict.md` §Gate Procedure states the general rule that an unanswerable destructive gate halts. harness's Restart option deletes `.harness/`, so the same rule applies to it | Adding the sentence edits item 2, and S3's AC-S3.3 depends on that block being byte-unchanged — it is the only evidence that the pre-harness legacy path was not silently made unreachable. Deferred rather than done, so the byte-comparison keeps its meaning. **Recorded here, not only in S3's commit messages**, because the ledger is what a later slice reads |
| **`/team-memory`'s store-location gate can only repair an ignore rule that lives in the repository root's own `.gitignore`, and only when an ancestor of `docs/harness/memory/` is itself ignored** — added 2026-08-24 by `review-fixes-v8-12` S4. Ignores coming from a nested `.gitignore`, `.git/info/exclude` or a global `core.excludesFile`, and patterns that match far outside the store (`*.md`, `**/memory/`, `docs/harness/memory/` itself, a per-category directory, a file glob under the store) are **detected and reported but not repaired** — the gate prints the lines to change and the user applies them | The delete-the-offending-line branch was measured to newly track every `.md` in a repository when the line was `*.md`, and installing the star cascade where no ancestor is ignored was measured to newly ignore `docs/other/` and `docs/harness/plan/`. A one-directional side-effect check passes in the first case, so the branch was dropped by explicit decision rather than guarded. Repairing the other sources is out of a per-repository skill's domain |
| **`/team-memory` still never runs `git add` or `git commit`** — added 2026-08-24 by `review-fixes-v8-12` S4. The `Store` line on the Save Report tells the user the command to run. So the conditional claim the same slice introduced ("git-committed unless that path is gitignored") is exact about the ignore rule and silent about the second condition: someone still has to commit | Deliberate and unchanged from the epic's own scope decision — a knowledge-capture tool creating commits the user did not ask for is surprising and hard to undo. Recorded because the description now states a condition, and a reader could take the stated one for the only one |
| **Three consequences of S4 that other slices must pick up** — added 2026-08-24 by `review-fixes-v8-12` S4. (a) `README.md` and `CLAUDE.md` still carry the unconditional claim; S4 fixed the 4 sites inside `skills/team-memory/` and `skills/memory/` only, because this epic assigns README to S7 and CLAUDE.md to S6. **Corrected 2026-08-24 by S4's own implementation cold review, before this row shipped:** an earlier revision of this row said "`README.md` (2 sites)" — measured, it is **3**. The third (`Git-committed, team-shared decisions, patterns, and conventions`, in the skills overview table) escapes a case-sensitive `grep git-committed`, which is exactly how the miscount happened; S7 must re-measure case-insensitively rather than trust a count. The same revision called the `CLAUDE.md` site an unconditional assertion — it is not: that sentence reports what the skill *declares*, so S6's fix is to restate the declaration, not to append a condition to it. **A fourth site was missed entirely** and is recorded here rather than in a new row, since it belongs to this same consequence: `templates/_shared/safety_guard.md` §Full output base cleanup states "`docs/` is git-ignored" as an unconditional fact in a warning **6 skills** render — it is a shared single source, so whoever fixes it must check all of its citing sites. (b) The epic's **AC-2.2 acceptance command** (`git check-ignore -q docs/harness/memory/`) is now a necessary but not sufficient test: it returns 1 — not ignored — while the record about to be written IS ignored, in all three measured blind spots (a file glob under the store, a per-category pattern, and a store with a tracked descendant, since `check-ignore` consults the index). The implemented gate checks the store, its README, each category directory and one record-shaped probe per category. (c) The epic's **S5 plan says `/harness doctor` reuses S4's gitignore verdict wording verbatim**; the implemented check takes the categories found by `save` Step 1 as input, so doctor needs its own argument-free form and must cite `skills/team-memory/SKILL.md` §Step 1.5 by name instead of restating it **Closed 2026-08-26 by `review-fixes-v8-12` S5**: doctor's item ⑤ has its own argument-free form and cites `skills/team-memory/SKILL.md` §Step 1.5 by name (items 1–2 only; items 3–6 do not apply), together with the §Step 1 Storage Criteria table that item 2 itself names. The argument-free form probes the store, its `README.md`, and each built-in category directory and record-shaped probe path — the two residual limits it cannot reach are recorded as their own row further down this table. | Each belongs to a slice that owns the file or the contract; recording them in commit messages alone is the half-updated-ledger failure this epic exists to close |
| **The pre-release live probe (e) cannot exercise two of S4's acceptance criteria** — added 2026-08-24 by `review-fixes-v8-12` S4. Probe (e) runs `/team-memory save` inside this repository, so it can never reach AC-2.4's not-a-git-repository path, and it must not take the `Un-ignore the store` branch, which would rewrite this repository's own `.gitignore` | Widening the probe means adding a scratch-repository scenario to the release gate — a change to the epic's release procedure, not to this slice. Flagged so "probe (e) passed" is not read as covering the whole gate |
| **Two gaps in `/team-memory`'s new store-location gate, known when it shipped** — added 2026-08-24 by `review-fixes-v8-12` S4. (a) The gate probes the categories that `save` Step 1 identified; if the user then answers `Edit` at Step 2's per-item gate and moves a record to a different category, that category was never probed, and the Save Report reports the outcome the probe found rather than the one that applies. (b) `clean` deletes records and rewrites the index without evaluating the gate at all, so it never tells the user its store is unshared | Re-probing on an `Edit` answer means running the gate from inside Step 2's loop, which is the shape §Step 1.5 exists to avoid; extending the gate to `clean` is a second sub-command's worth of design. Both were weighed and left, and are recorded here because a plan document under `docs/` is not something a later slice can read |
| **`/harness doctor`'s team-memory check has two residual blind spots** — added 2026-08-26 by `review-fixes-v8-12` S5. Item ⑤ probes the store directory, its `README.md`, each built-in category directory and one record-shaped path per category. It still cannot see (a) user-defined categories, which `skills/team-memory/SKILL.md` Key Rule 7 allows and which only `save` Step 1's own output enumerates, and (b) the *Known limit* §Step 1.5 item 2 records for itself — an ignore pattern aimed at the free-form slug after a category prefix. doctor discloses both in its output rather than reporting a clean verdict | Closing either one needs an input doctor does not have without running `save`; naming the limit in the output is the honest form until a slice owns that input |
| **`/harness doctor` item ② reports this session's tool list, not the plugin's actual availability** — added 2026-08-26 by `review-fixes-v8-12` S5. `templates/_shared/mode_gate.md` rule 2 is an observational condition, so doctor reports it as observed and says so. It therefore cannot diagnose another session's tool scope, nor the `disallowed-tools` turn-scope leak this repository has hit and never established a mechanism for | An active probe would contradict the same rule the item cites; the leak's mechanism is UNVERIFIED, so there is nothing to test against yet |
| **`/harness doctor` cannot tell a fresh install from a stale in-process skill body** — added 2026-08-26 by `review-fixes-v8-12` S5. Items ① and ④ read the two install locations on disk. The failure this repository hits most often is a different one: the copies on disk are current while the process keeps running the body it loaded at start-up, which `/clear` does not refresh. A green install check is therefore not evidence that the running skill body is current, and doctor says so beside items ① and ④ rather than letting a tick read as reassurance | Diagnosing it needs a value the running process does not expose; what S5 shipped is the `${CLAUDE_PLUGIN_ROOT}` axis, which narrows *which copy* is compared, not *which body* is executing |
| **`/harness doctor` item ④ (c) excludes runtime directories by denylist, so a new one surfaces as noise** — added 2026-08-26 by `review-fixes-v8-12` S5. The right-hand side is the comparison copy's whole file set minus `.git/` and `.in_use/`. If the engine later creates another runtime directory under an install root, its paths print as present-in-the-install-copy-only and unrecognised. That is noise, not a miss — and the printed path is itself the signal to update the denylist | A whitelist of content directories would silently miss a new top-level directory, which is the worse failure; the denylist's ageing is visible by construction |
| **`skills/harness/SKILL.md` §Session Boundary's inventory does not know its own non-applicable paths** — added 2026-08-26 by `review-fixes-v8-12` S5; **S7 owns the follow-up.** That section records exclusions by hand (`excludes the commit-failure abort path`), while `doctor` declares its own non-applicability inside its own section instead. With two such declarations living apart, a third one added later leaves the inventory quietly non-exhaustive | S5 deliberately does not edit the §Session Boundary section — its insertion budget is five sites and that section is not one of them, so the reconciliation belongs to the slice that owns this file's ledger |
| **The install-copy drift itself is not closed by `/harness doctor`** — added 2026-08-26 by `review-fixes-v8-12` S5. doctor reports the comparison and writes nothing; the re-sync that actually clears the drift belongs to the release steps (the pre-ship live-probe gate, then S7). The "Repository ↔ installed plugin copy drift" row above therefore stays open after S5 ships | A diagnostic that repaired what it measured would break the read-only contract the whole slice is built around |
| **`CHANGELOG.md`'s `## [Unreleased]` still carries a stale marker-site total for the `session-conflict` group** — added 2026-08-26 by `review-fixes-v8-12` S5; **S7 owns the correction, which is an in-row amendment, not a rewrite.** The entry reads `Nine groups, 48 marker sites.` while a later entry in the same `## [Unreleased]` block says that group's floor rose, and `python scripts/verify_sync_markers.py` reports a different site total today. S5 records the contradiction rather than editing another slice's ledger entry | Correcting it quietly from S5 is the half-updated-ledger move this epic exists to close; the entry was written by S3/S4, S7 owns the fix, and it has to land before the release ships |
| **`.gitattributes` does not cover `*.md`, so what this repository commits as line endings depends on each machine's `core.autocrlf`** — added 2026-08-26 by `review-fixes-v8-12` S5; **S7 owns the follow-up.** The attributes file pins `*.workflow.js`, `*.sh` and `*.yml` to `text eol=lf` and nothing else, so every file this slice touches reports `i/lf  w/crlf  attr/` on a Windows checkout. The committed blobs are LF because `core.autocrlf` is `true` here, which is the Git-for-Windows installation default rather than anything this repository states; on a checkout where it is `false`, a CRLF blob is what gets committed | The fix is one line, `*.md text eol=lf`, plus a single `git add --renormalize .` commit that rewrites every `.md` in the repository — a blast radius no five-file slice can carry, and S5 deliberately keeps `.gitattributes` out of its files_touched |

## v8.9 — Shipped (2026-08-05) — `/study` skill + `/handoff` write-immediately

- **`/handoff generate` no longer asks before saving** — Step 3 resolves the path, Step 4 writes.
  The gate guarded a write that is safe by construction (the filename convention never
  overwrites; the write touches no git state, no `.harness/`, nothing outward-facing; and
  `generate` is already an explicit user action). The correction affordance it provided is
  preserved by inverting the order — the file is the preview, and the write report always names
  how to correct it. `Cancel` is given up and documented as given up; the Progress Ledger's
  broken-chain warning moved into the write report. `resume` keeps its own gate.
  See `skills/handoff/SKILL.md`.
- **`/study` — 9th multi-path skill** — turns a `/harness` output directory, a whole project,
  or a git diff into a 7-section, 3-tier study guide (concept explanation / real code excerpts
  / interview Q&A / hands-on exercises / design rationale / anti-patterns / glossary) for
  post-hoc learning. Quick (inline, full path — the Windows CRLF safety net) or deep/thorough
  (3 evidence lenses + per-topic-bucket authors + a thorough-only reproducibility critic +
  assemble, native Workflow path, opt-in gated). Provenance is machine-checked, not a prompt
  promise: every repo-sourced code excerpt is re-read against the actual file before
  publishing, and every narrative claim carries a `repo`/`inference` basis that auto-downgrades
  when its evidence path does not resolve. Ships a self-contained static HTML report (inline
  CSS/JS, dark/light aware, tier-filterable, zero external requests) — the first non-`.md`
  skill asset in this repository (`templates/study/html_shell.html`). Carries all 3 SYNC-WITH
  marker groups (`§Ambiguity Prompt`, `§agent-harness-defaults`, `§Ad-hoc Dispatch Contract`);
  `scripts/verify_sync_markers.py` floors raised accordingly. See `skills/study/SKILL.md`.

## v8.8 — Shipped (2026-07-28) — Anthropic best-practices gap analysis — epic continuity

Scope selected from a gap analysis against Anthropic's official Claude Code best-practices
document (`docs/harness/anthropic-best-practices-skill-improvements/anthropic-best-practices.md`),
targeting the reported pain point: epic-scale work done through `/harness` ends each session
"ambiguously incomplete" because session-boundary output, artifact preservation, and the
`/harness` ↔ `/handoff` handoff were each only partially specified. Design principle: `/harness`
stays the slice executor (unchanged state machine, unchanged `state.json` v3 schema); `/handoff`
becomes the epic ledger — all changes are contract precision (tables/states/gates), not new
subsystems.

- **`## Session Boundary` single source + Step 8 evidence-preservation fix** — every
  `/harness` session-ending point (4 phase boundaries + the L1 max-retry "Stop" + Step 8) now
  prints one standardized block (resume command, `{docs_path}`, `/handoff generate`
  recommendation); Step 8's recommended "Commit code only" option no longer deletes
  `{docs_path}` (previously silently destroyed `spec.md`/`qa_report.md` in `.gitignore`d
  `docs/` setups — this repo's own default). See `skills/harness/SKILL.md`.
- **`/harness` → `/handoff` wiring + optional epic Progress Ledger** — `/harness` recommends
  `/handoff generate`; `generate` records task state in a fixed-label parse-anchor format;
  `resume` cross-checks it against the live `.harness/state.json` (report-only); `generate`
  gains an optional `## Progress Ledger` (Epic/Slice/Status/Evidence/Notes) that survives across
  an epic's multiple HANDOFF documents via an Epic-matching carry-forward rule. See
  `skills/handoff/SKILL.md`.
- **`/harness` branch-reuse safety + Setup Summary verification-gap warning** — a pre-existing,
  non-empty `harness/<slug>` branch is never silently reused (diff-contamination risk); Setup
  Summary warns (never halts) when all 4 verification commands are `null`. See
  `skills/harness/SKILL.md`.
- **`/spec` cross-skill session-conflict gate** — symmetric to `/harness`'s own gate; closes a
  real (not hypothetical) silent-overwrite path where `/spec` would delete a live `/harness`
  session's `state.json` with no confirmation. See `skills/spec/SKILL.md` §Session Recovery.
  **Correction — no longer symmetric (review-fixes-v8-12 S3):** `/harness` §Session Recovery
  item 1 is now a branch table that gates a mismatched `skill`, gates an absent one when
  `version` is `"3.0"`, and defers the rest to its legacy branch, while `/spec` still folds the
  absent case in. Both now cite `templates/_shared/session_conflict.md` §Gate Procedure, which
  records the difference. The rest of this row is unchanged and still true.
- **`/deep-review --spec <path>` opt-in Spec Conformance pass** — an independent report section
  checking the diff against a spec's Acceptance Criteria/Scope; the defect reviewers stay
  spec-blind (anchoring invariant unconditional); only upgrades `## Assessment`, never
  `## Statistics`/`## Round Verdict`. See `skills/deep-review/SKILL.md`.
- **`/spec` fresh-session recommendation** — the Phase 3 "start implementation" prompt defaults
  to printing the exact `/harness --output-dir ...` command for a NEW session, per the
  reference document's Phase 0 guidance, alongside the previous immediate-invoke option. See
  `skills/spec/SKILL.md` §Phase 3.

**Deferred to v8.9+ (documented, not silently dropped):**

| Item | Why deferred |
|------|-------------|
| **P2-1** — non-destructive "view state only" option in `/harness` Session Recovery's Resume/Restart/Stop gate; explicit priority rule between Session Recovery and the no-args next-step suggestion | Real gap (2 of 3 existing options are destructive), but additive to the same gate rather than blocking the epic-continuity fixes above. **First half closed 2026-08-19 (v8.11.0 batch)**: `skills/harness/SKILL.md` §Session Recovery item 7's default (`Otherwise`) gate branch now offers a 4th, non-destructive `View state only` option (reusing item 7-(b)'s "`.harness/` is NOT deleted" description phrasing; the label `View state only` is new and carries no `(Recommended)` marker) that prints the standard status block (§Standard Status Format, cited by name — its `Path` row already carries `path_resolved`, not repeated below) plus fields that block does not carry (`docs_path`, `verify.layer1_retries`/`layer2_retries`, `autofix.applied`, whether `epic.boundaries` is set, `plan_critic.applied`+`counts`) and then halts — no loop, no state.json write. Scoped explicitly to that one branch: item 7-(b) docs_path drift is unmodified and does not offer it, and item 7-(a) Epic residue does not offer it either — its option set is unchanged, though its `same labels/actions as below, minus "Resume"` parenthetical was extended to `minus "View state only"` in the same commit, because the 4th option would otherwise have made that cross-reference false (see the new row above on the other 6 skills' gates, which this batch also leaves unmodified). The second half of this row — the Session Recovery ↔ no-args next-step priority rule — is still open, see the new row above |
| **P2-2** — state-machine-level distinction between the 3 different roads that all currently converge on `phase: "completed"` (QA PASS / Accept-as-is / Max-rounds-reached) | The Session Boundary block (v8.8, above) already derives a display-only `Reason` label from existing fields without a schema change; a true state-machine fork would require a `state.json` version bump, which is out of scope until real usage shows the display-only mitigation is insufficient |
| **P2-3** — top-of-file invariant summary in `skills/harness/SKILL.md` (~75KB/1,090 lines when this row was written; now ~190KB/2,237 lines in HEAD post `harness-handoff-coldreview-epic-slice` (2026-08-19 measurement) — figure not updated in place, flagged stale here rather than silently left wrong, see that epic's slice-f traversal record. **Re-measured again 2026-08-19 (v8.11.0 batch, working tree, base `f32c3fb`, after this same batch's own B4+B7 edits to this file): 2,288 lines** — re-measure with `wc -l skills/harness/SKILL.md` rather than trusting any number on this row, including this one; this batch's own edits moved it again in the same commit that corrects it. **Re-measured a third time 2026-08-19 (v8.11.0 batch, after every body edit landed in this batch — the original B4+B7 edits plus this batch's own QA rounds 1–6 fixes (round 3 = the Layer 3 pass, rounds 4–5 = two adversarial re-verification passes, round 6 = the pre-ship cold review) — working tree, base `f32c3fb`): 2,293 lines** — this figure is only as fresh as the last body edit to that file in this batch) with Key Rules/Architecture Principles at the bottom — a compact-survival risk | Needs to re-evaluate alongside M3 (template compression, below) rather than as an isolated insertion; the fixed SKILL.md section-order convention means this needs its own design pass, not a drive-by edit |

## v8.7 — Shipped (2026-07-24) — model tiering + session continuity

Scope selected from the 2026-07-24 three-project usage audit (25-agent workflow; evidence: the user
operates primarily as a free-form ultracode orchestrator — the plugin's highest-value additions are
contracts for that path plus session continuity):

- **`frontier` model preset** — Fable evaluator / Opus advisor / Sonnet executor / Haiku verifier;
  judgment agents (cross-verification, critic) remapped to the evaluator role (no-op for pre-8.7
  presets). See `templates/_shared/model_config.md`.
- **Project defaults** — `agent-harness-defaults: path=..., model-config=..., verifier-model=...`
  line in a defaults source — `.claude/settings.local.json` env (recommended) / project CLAUDE.md /
  `~/.claude/CLAUDE.md`, first wins wholesale — as a standing opt-in (Mode Gate §Ambiguity Prompt
  step 4.5); kills the per-session `/effort` + model-picker ritual. See `templates/_shared/project_defaults.md`.
- **Ad-hoc Dispatch Contract** — output-language + model-routing rules for non-template sub-agents
  and ad-hoc Workflow scripts; root-cause fix for the v8.6.0 English leak. See
  `templates/_shared/adhoc_dispatch.md`.
- **`/handoff` skill** — human-gated session handoff (generate / resume with git-drift verification /
  list); replaces the 150+ hand-written HANDOFF/NEXT_SESSION documents found across the three
  audited projects. See `skills/handoff/SKILL.md`.
- **`/deep-review` round bookkeeping (P05, reduced scope)** — standardized round numbering,
  orchestrator-only prior-finding reconciliation (reviewers stay blind), advisory Round Verdict;
  no auto-convergence loop (the /spec oscillation invariant is untouched — user re-invokes per
  round). See `skills/deep-review/SKILL.md`.
- **`/spec` Review Sheet + `/spec digest` (P15)** — specs open with a derived review sheet (TL;DR,
  decision table, open questions, changed-in-this-revision); `digest` produces a read-only 3-layer
  briefing of existing docs (evidence: spec-comprehension effort was a reported top pain).
  See `skills/spec/SKILL.md`.

M4 (persona override), M3 (template compression), and L1 (external CLI wrapper) remain planned
(see the v8.3+ table below); audit follow-ups not in this scope (cold-review preset, segment
checkpoints, campaign ledger, `harness clean/archive`) are tracked for v8.8+.

---

## v8.x — Shipped

**v8.6.0** — Mode Gate §Ambiguity Prompt + §Path Transparency

- **§Ambiguity Prompt (single source `templates/_shared/mode_gate.md`, wired into all 8 multi-path skills)**: when no `--mode` is given and ultracode is OFF (Workflow engine available, interactive session), skills explicitly ask inline-vs-workflow instead of silently auto-resolving. `--no-prompt` / non-interactive sessions keep silent auto-resolution.
  See: `templates/_shared/mode_gate.md`, `skills/{harness,spec,debug,deep-review,codebase-audit,migrate,refactor,test-gen}/SKILL.md`
- **§Path Transparency**: every skill prints `Path : <inline|workflow> (<reason>)` — the chosen execution path and its cause are always visible, including on the auto-resolved and ultracode paths.
- **SYNC-group tracking (`scripts/verify_sync_markers.py`)**: the §Ambiguity Prompt marker is tracked as a SYNC group — referential integrity of the marker plus a minimum-site-count floor across the wired skills, so a dropped `SYNC-WITH` marker fails the lint. Content-level drift of each skill's wiring vs the single source, and a §Path Transparency marker group, are follow-ups (not yet enforced).
  See: `scripts/verify_sync_markers.py`

---

**v8.5.0** — Native Workflow Reframe + Skill Renames (alias-preserved) + Mode Gate

- **Skill renames (alias-preserved, NOT a hard break)**: `/code-review`→`/deep-review`, `/memory`→`/team-memory`, `/workflow`→`/harness`. Old names retained as deprecation-stub skills (frontmatter `name:` kept for discovery) that resolve, print a localized notice, and redirect. Removal no earlier than the next MAJOR.
  See: `skills/{deep-review,team-memory,harness}/SKILL.md`, stubs in `skills/{code-review,memory,workflow}/SKILL.md`, README "Skill Naming & Built-in Command Relationship".
- **Mode Gate (derived; opt-in)** — `templates/_shared/mode_gate.md`: default = inline path; the native Workflow path runs only when the Workflow tool is available AND the session opts in (ultracode OR explicit `--mode`); `has_git == false` or a missing/erroring engine falls back to inline. Replaces the previous raw-effort gating idea and the mode-selection AskUserQuestion roundtrip.
- **`disallowed-tools` frontmatter** on every skill — runtime enforcement of read-only / no-escalation contracts (verified-safe subset; e.g. `/team-memory` blocks Task/Agent/Workflow/WebSearch/WebFetch; read-only review skills block Edit/Write).
- **Native Workflow authoring (8 high-overlap skills)**: at opt-in depth, `harness`/`spec`/`debug`/`deep-review`/`codebase-audit`/`migrate`/`refactor`/`test-gen` author & run shipped segment scripts (`${CLAUDE_PLUGIN_ROOT}/workflows/<skill>.<segment>.workflow.js`) that fan out via `parallel()`/`pipeline()`, return schema-validated objects, and resume via `runId`. Canonical schemas (`workflows/_reference/schemas.md`, 16 `##` headings = 18 names): AnalysisResult, PlanResult, ChangeSet, VerifyVerdict, CriticReport, Hypothesis, DebugAnalysis, RootCause, Finding, FindingSet, CrossVerifyReport, MigrationPlan, AuditAnalysis, CompletenessCritique, AuditResult, SkepticVote, MutationVerdict, ExecutedMutation. (RefactorPlan is an in-script extension of PlanResult, not a canonical heading.) The 3 HARD-GATEs (spec-confirm / verify-fail / auto-fix-apply) stay in the orchestrator BETWEEN segments — `scripts/verify_meta_literal.py` rejects any gate token inside a script.
  See: `workflows/*.workflow.js`, `workflows/_reference/schemas.md`, `scripts/{verify_meta_literal.py,check_workflow_syntax.mjs,verify_block_sync.py}`.
- **`/deep-review --comment` / `--fix` parity**: `--comment` posts inline PR comments (after an explicit confirm); `--fix` applies critical/major suggestions to the working tree behind a HARD-GATE (never commits/pushes).
- **`templates/_shared/` single-source extraction** (Mode Gate, Input Trust Model, model_config, Safety Guard, detection table, status format, spec-context, falsification rules, AskUserQuestion) + `scripts/verify_block_sync.py` simplified to single-source + SHA256 reference checks.

**Breaking changes (alias-mitigated)**: command names change to `/harness`, `/deep-review`, `/team-memory` — old names still resolve via deprecation stubs (no invocation breaks), but muscle-memory and docs should migrate. Opt-in Workflow-path runs execute a segment script rather than inline prose orchestration; that path's resume key is the engine `runId` (same-session) with the `state.json` phase machine for cross-session — pre-8.5 interrupted sessions should Restart rather than Resume.

---

**v8.4.0** — Spec Skill Hardening + /ship merge-to-base

- **A+B+C+D — /spec Hardening**: Risk Auditor + Tech Constraint Analyst added to deep mode (4-analyst parallel synthesis), Spec Critic stage with 3-way gate + 1-round auto-revise, Convention Scan with `--reference` flag and has_git=false candidate file detection, qa_notes / critic_findings / conventions persistence to `{docs_path}` for slug-safe /spec → /workflow handoff (workflow Step 1.5 reuse, Step 2 variable injection into 4 planner templates, Step 8 cleanup protection).
  Token cost (deep mode): ~1.9x of base (analytic estimate from dispatch count: 5 calls deep vs 3 calls pre-8.4 + per-call content growth; live measurement deferred to follow-up smoke session). The estimate **includes** the Input Trust Model boilerplate (~7 lines / ~150 tokens) duplicated across 8 templates (4 planners + 4 spec analyst templates) for security clarity — this is a deliberate trade-off: ~1680 tokens of overhead per /spec deep run buys uniform prompt-injection guards on every sub-agent dispatch, judged worth it over a 1-line compressed form that risks ambiguity. Spec-time detection rate (coin-washer reproduce, analytic mapping of 4-analyst + Critic against original review_report.md): **Critical 5/7** (high-confidence catches C1+C2+C4+C5+C6; borderline C3+C7), **Major 6/12** (high-confidence M1+M2+M3+M4+M5+M14). False-positive on 2 known-good specs (`feature-coin-washer-improvements`, `qaas-411-reopen-fix` — analytic prediction): **Critical=0, Major≤1** each.
  See: `skills/spec/SKILL.md`, `templates/spec/{risk_auditor,tech_constraint_analyst,critic,synthesis}.md`, `templates/planner/*.md`
- **N2 — /ship Stage 6.5 (`merge_to_base`)**: Adds `release_branch → base_branch` merge step between 6c-i (branch push) and 6c-ii (tag push), ensuring the tag is reachable from `base_branch`. Branch protection auto-detection with PR creation fallback. Substep-level recovery via 3 new enum values (`merge_base_pending` / `merge_base_done` / `merge_base_pushed`). **dogfood: planned for 8.4.0 release (develop → main)**; the actual /ship invocation that produces v8.4.0 will exercise Stage 6.5 end-to-end and the executed path (Path A vs Path B, FF vs Non-FF) will be appended here in the release session as evidence (closes m11 — placeholder until release session updates this line).
  See: `skills/ship/SKILL.md §6.5`

**Breaking changes**: deep mode persona count 2 → 4 + Critic; /spec Phase 3 handoff CLI contract gains `--output-dir`; planner templates gain `{qa_discovery_notes}` / `{critic_findings}` placeholders (forked custom templates may render empty Discovery Notes silently); `/spec` deep mode now hard-error halts at Phase 2a-D step 3 if `state.conventions` is null on resume — pre-8.4 sessions that reach `qa_complete`/`gen_ready`/`qa_active` MUST complete Step 1.5 (Convention Scan) before Phase 2 entry, either by Restart or via the M14 backward-compat Resume description override which routes through Step 1.5 first; intentional asymmetry vs `/workflow`'s silent-default missing-field policy (rationale: 4-analyst pipeline depends on convention context for high-quality output, so silent degradation through 8.4 flow would produce lower-quality specs).

---

**v8.3.0** — Ship version_bump auto-detection for `.claude-plugin/*.json`

- **N1 — `/ship` version_bump auto-detection for `.claude-plugin/*.json`**: Stage 2 (`version_bump`) now auto-detects version fields in `.claude-plugin/plugin.json` (top-level `$.version`) and `.claude-plugin/marketplace.json` (`$.metadata.version` + `$.plugins[*].version`) alongside the existing standard package manifests. Pass 2 uses JSON parsing on exact key paths to avoid regression where naive string replace would taint other fields (e.g., a `description` containing the same version string). Original line-ending (CRLF vs LF) and trailing-newline state are preserved.
  See: `skills/ship/SKILL.md §Step 2 Stage — version_bump (Pass 1 + Pass 2)`

---

**v8.2.0** — Ship security hardening: Safety Guard parity + tag-length bound

- **S1 — Ship Safety Guard parity with /workflow**: `.harness/` cleanup now performs unconditional symlink-escape check (`Path.resolve() ⊆ Path.cwd()`, no `has_git` gating) and prints the exact absolute target path before deletion, mirroring the `/workflow` Artifact Cleanup Safety Guard.
  See: `skills/ship/SKILL.md §Step 8 Cleanup (Safety Guard)`, `skills/workflow/SKILL.md §Step 8 Artifact Cleanup Safety Guard`
- **S2 — Tag-name length bound**: Tag regex tightened from `^v?[0-9a-zA-Z][0-9a-zA-Z._-]*$` to `^v?[0-9a-zA-Z][0-9a-zA-Z._-]{0,253}$` (254-char max), rejecting pathological large inputs.
  See: `skills/ship/SKILL.md §Step 6c Push (Input validation)`

---

**v8.1.0** — Release hardening: Path Validator + state invariants + schema parity

- **H1 — Verifier model flexibility**: Layer 1 Mechanical Verification primarily runs commands and parses exit codes, so haiku is sufficient by default. An opt-in override is provided for high-cost diagnosis (concurrency, complex test failures).
  See: `skills/workflow/SKILL.md §Step 1 Setup` (--verifier-model), `§Model Selection`
- **H2 — Auto-fix proposal**: When Layer 1 still fails after 3 retries, users may review an AI-proposed diff via a HARD-GATE-based single attempt. The same flow is added to `/refactor` for test regressions.
  See: `skills/workflow/SKILL.md §Step 5 Verify Phase`, `§Architecture Principles`
- **Release hardening** (post-review): `## Architecture Principles` section, `## Path Validator` single-source path validation (applies to `--output-dir`, `{failing_files_list}`, unified diff, Safety Guard), Auto-fix State Transition Table with invariants I1–I4 (once-only, retry clamp, session-resume entry point), `/workflow` ↔ `/refactor` schema parity (`verify.autofix_attempted` nested, reader-union backward compat), 2nd HARD-GATE UX unified across skills.
  See: `skills/workflow/SKILL.md §Architecture Principles`, `§Path Validator`, `§State Machine`
- **L2 — ROADMAP**: This file. Transparent planning and decision log.
- **L3 — `--output-dir` flag**: Supports monorepo/CI scenarios requiring artifacts outside the default `docs/harness/`. Path validation rules live in the Path Validator single source.
  See: `skills/workflow/SKILL.md §Step 1 Setup` (--output-dir), `§Architecture Principles §Path Validator`
- **M2 — `.github/` templates**: `bug_report.yml`, `feature_request.yml`, `question.yml` issue templates + `PULL_REQUEST_TEMPLATE.md` to lower contribution barrier.
- **M2 — Marketplace description**: `plugin.json` and `marketplace.json` descriptions updated with value-proposition language and "multi-agent" keywords.
- **M1 (partial) — README text demo**: "At a Glance" section with before/after examples, terminal output sample, token cost table expansion.

---

**v8.0.0** — Thin Orchestrator + 3-Layer Quality Gates

- Thin Orchestrator: state-machine orchestrator that passes paths only to sub-agents (no accumulation), 1-line return parsing, 40–60% token savings vs fat orchestrator
- 3-layer quality gates: Layer 1 mechanical (build/test/lint/type-check/TODO scan), Layer 2 structural (criterion mapping, scope validation), Layer 3 LLM judgment (bias-reduced, context-isolated)
- `state.json v2.0`: interrupt-safe session recovery, round-based retry loop, run-style (auto/phase/step)
- Convention scanner sub-agent, multi-mode planner (single/standard/multi), phase-mode execution

---

## v8.3+ — Planned

Items deferred from v8.1 / v8.2 with rationale:

| Item | Reason deferred | Notes |
|------|----------------|-------|
| **M4 — Custom persona override** (`templates/user-override/`) | Variable contract definition required first; ROI analysis pending real usage data | Architect/Senior split: Senior recommended deferral. Minimum viable: `.harness/templates/` project-level override only |
| **M3 — Template compression** | Senior measured actual templates: avg 46 lines, max 185 lines (~2–3k tokens). Feedback premise of "8–12k tokens" did not match measurements | Re-evaluate after v8.1 usage data |
| **L1 — External CLI wrapper** | Claude Code's `/skill` invocation already functions as CLI; separate repo adds maintenance burden disproportionate to value | Reconsider if community demand emerges |

> **Re-deferred to v8.7+:** the v8.5.0 native-Workflow reframe (skill renames + Mode Gate + native authoring) was prioritized over the items above per the 2026-06-02 optimization review (ROI reversed once the native engine landed). M4 (persona override), M3 (template compression), and L1 (external CLI wrapper) remain planned for v8.7+. N2 (`merge_to_base`) shipped in v8.4.0 and has been removed from this table.

### Residual review gaps (post-v8.1 verification)

Verified residual items from `/ship` skill review (`docs/harness/unstaged-changes/review_report_final.md`). Critical/Correctness items from v8.1 were resolved in `11c4d5e`. **S1 and S2 shipped in v8.2.0** (see Shipped section above). **N1 shipped in v8.3.0** (`.claude-plugin/*.json` auto-detect, see Shipped section above). No further residual review gaps remain at this time.

---

## Non-Goals

Explicitly out of scope for agent-harness regardless of version:

- **Non-Claude LLM support**: agent-harness is designed for Claude Code's tool ecosystem (AskUserQuestion, sub-agents). Supporting OpenAI/Gemini/local models would require a different architecture.
- **Git-free-only dedicated CLI**: git-free mode is already supported (auto-detected). A separate non-git CLI adds complexity without proportional benefit.
- **Automatic code application without HARD-GATE**: All AI-generated diffs require explicit user confirmation before apply. Auto-apply is permanently out of scope.
- **Internet-dependent features as required dependencies**: WebSearch is used opportunistically (migrate skill). No feature should fail hard when offline.
